import numpy as np
import math
import os
from models.net_builder import net_builder
from config import cnv_single_config

# args = cnv_single_config()
# model = net_builder(name='cpfnet', in_channels=1, n_class=args.num_classes).cuda()
# print('parameters_number = {}'.format(sum(p.numel() for p in list(model.parameters()) if p.requires_grad)))

a = [1,2,3,4,5]
print(*a)
# print(a)